<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <artifactId>tools.deps.config.edit</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>tools.deps.config.edit</name>

  <parent>
    <groupId>org.clojure</groupId>
    <artifactId>pom.contrib</artifactId>
    <version>1.4.0</version>
  </parent>

  <developers>
    <developer>
      <id>jarrodctaylor</id>
      <name>Jarrod Taylor</name>
    </developer>
  </developers>

  <properties>
    <!-- used for build -->
    <clojure.warnOnReflection>true</clojure.warnOnReflection>
    <clojure.version>1.12.5</clojure.version>
  </properties>

  <repositories>
    <repository>
      <id>clojars</id>
      <url>https://repo.clojars.org/</url>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>org.clojure</groupId>
      <artifactId>clojure</artifactId>
      <version>${clojure.version}</version>
    </dependency>
    <dependency>
      <groupId>org.clojure</groupId>
      <artifactId>tools.deps.config</artifactId>
      <version>0.1.3</version>
    </dependency>
    <dependency>
      <groupId>rewrite-clj</groupId>
      <artifactId>rewrite-clj</artifactId>
      <version>1.2.54</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
      <!-- By default, compile everything as a sanity check, but do
           not include any AOT-compiled .class files in the
           JAR. Projects may override as needed. -->
      <groupId>com.theoryinpractise</groupId>
      <artifactId>clojure-maven-plugin</artifactId>
      <version>1.7.1</version>
      <extensions>true</extensions>
      <configuration>
        <warnOnReflection>${clojure.warnOnReflection}</warnOnReflection>
        <temporaryOutputDirectory>true</temporaryOutputDirectory>
      </configuration>
      <executions>
        <execution>
          <id>clojure-compile</id>
          <phase>none</phase>
        </execution>
        <execution>
          <id>clojure-test</id>
          <phase>test</phase>
          <goals>
            <goal>test</goal>
          </goals>
        </execution>
      </executions>
      </plugin>
    </plugins>
  </build>

  <scm>
    <connection>scm:git:git@github.com:clojure/tools.deps.config.edit.git</connection>
    <developerConnection>scm:git:git@github.com:clojure/tools.deps.config.edit.git</developerConnection>
    <url>git@github.com:clojure/tools.deps.config.edit.git</url>
    <tag>HEAD</tag>
  </scm>

</project>
